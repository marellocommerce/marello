{% extends '@OroAction/Operation/form.html.twig' %}

{% block form %}
    {% set buttonOptions = operation.definition.buttonOptions %}
    <div class="widget-content">
        {{ form_start(form, {'action': app.request.uri, 'attr': {'id': form.vars.id, 'data-collect': 'true', 'class': 'form-dialog'}}) }}
        <div class="form-horizontal">
            <p>{{ 'marello.invoice.action.send_invoice_email.popup_description'|trans }}</p>
            <div>
                <div style="float:left">{{ form_row(form.send_customer_email, {'label': ' '}) }}</div>
                <div style="float:left;margin-left:-265px">{{'marello.invoice.action.send_invoice_email.customer_email'|trans({'%email%': form.send_customer_email.vars.relatedEntityEmail})}}</div>
            </div>
            {% if form.send_company_email.vars.show_field == true %}
                <div>
                    <div style="float:left">{{ form_row(form.send_company_email, {'label': ' '}) }}</div>
                    <div style="float:left;margin-left:-265px">{{'marello.invoice.action.send_invoice_email.company_email'|trans({'%email%': form.send_company_email.vars.relatedEntityEmail})}}</div>
                </div>
            {% endif %}
        </div>
        <div class="widget-actions">
            <button class="btn" type="reset">{{ (options.cancelText|default('Cancel'))|trans }}</button>
            <button class="btn btn-success" type="submit">{{ (options.okText|default('Submit'))|trans }}</button>
        </div>
        {{ form_rest(form) }}
        {{ form_end(form) }}
        {{ oro_form_js_validation(form) }}
    </div>
{% endblock %}
